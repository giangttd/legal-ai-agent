version: "3.8"

services:
  db:
    image: pgvector/pgvector:pg15
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-legalai}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-legalai2026}
      POSTGRES_DB: ${POSTGRES_DB:-legalai}
    ports:
      - "${DB_PORT:-5432}:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./database/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-legalai}"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      # Database (internal Docker network)
      SUPABASE_DB_HOST: db
      SUPABASE_DB_PORT: 5432
      SUPABASE_DB_PASSWORD: ${POSTGRES_PASSWORD:-legalai2026}
      DB_NAME: ${POSTGRES_DB:-legalai}
      DB_USER: ${POSTGRES_USER:-legalai}
      DB_SSL_MODE: disable
      # Auth — REQUIRED (no weak defaults; compose fails fast if unset). Generate:
      #   SECRET_KEY=$(openssl rand -hex 32)  JWT_SECRET=$(openssl rand -hex 32)
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY must be set in .env}
      SUPABASE_JWT_SECRET: ${JWT_SECRET:?JWT_SECRET must be set in .env}
      # AI - At least one required
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      GOOGLE_API_KEY: ${GOOGLE_API_KEY:-}
      # Supabase Storage (optional) — no hardcoded project default; blank = local storage
      SUPABASE_URL: ${SUPABASE_URL:-}
      SUPABASE_SERVICE_KEY: ${SUPABASE_SERVICE_KEY:-}
    volumes:
      - uploads:/app/uploads
      - logs:/app/logs
    depends_on:
      db:
        condition: service_healthy

volumes:
  pgdata:
  uploads:
  logs:
